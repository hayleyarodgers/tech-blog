<!-- Loop over each post in the posts array -->
{{#each posts as |post| }}
<div class="container-fluid">
    <div class="row justify-content-center">
        <div class="card rounded-0 col-11 my-4 px-0">
            <div class="card-body p-5">
                <blockquote class="blockquote mb-0">
                    <p>{{post.date_created}}</p>
                    <br>
                    <h2>{{post.title}}</h2>
                    <br>
                    <p>{{post.content}}</p>
                    <footer class="blockquote-footer">Written by <cite title="Source Title">{{post.user_id}}</cite></footer>
                    <br>
                    <br>
                    <a href="/post/{{post.id}}"><p>Read more →</p></a>
                </blockquote>
            </div>
        </div>
    </div>
</div>
{{/each}}
